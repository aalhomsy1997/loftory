import {StyleSheet} from 'react-native';
export const BaseStyles = StyleSheet.create({
  flexRowContainer: {
    display: 'flex',
    flexDirection: 'row',
  },
  flexColumnContainer: {
    display: 'flex',
    flexDirection: 'column',
  },
});
